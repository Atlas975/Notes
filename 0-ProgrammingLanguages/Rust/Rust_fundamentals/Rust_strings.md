# Rust strings
- Strings in rust are not null terminated like other languages and can contain null terminators as part of the string literal


# Rust string method
```
myWord.trim();  // remove whitespace 
```